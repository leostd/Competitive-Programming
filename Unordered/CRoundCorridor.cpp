#include "../library/lib.hpp"
#include <iostream>

using namespace std;

class CRoundCorridor {
public:
	void solve(std::istream& in, std::ostream& out) {
		ll n, m, q, x = 1e18;
		in >> n >> m >> q;
		
		out << x << endl;
		
		while(q--){
			ll sx, sy, ex, ey;
		}

	}
};