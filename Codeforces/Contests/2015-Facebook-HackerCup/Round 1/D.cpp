#include <bits/stdc++.h>
#define N 2000007
using namespace std;

int t, n;
int p[N], g[N];

int main()
{
    scanf("%d", &t);
    for(int i = 1; i <= t; ++i)
    {
        scanf("%d", &n);
        for (int j = 0; j < n; ++j)
            scanf("%d", &p[j]);
    }
}
