#include <stdio.h>
#include <algorithm>
#include <stdlib.h>
#include <math.h>
#include <iostream>
#include <string.h>
#include <vector>
#define MAXN (int)10e5+9
using namespace std;

typedef vector<int> vi;

int numbers[MAXN];
int n, q;

int main()
{
    while(scanf("%d %d", &n, &q) != 2){
        for (int i = 0; i < n; i++)
            scanf("%d", &numbers[i]);
        
    }
}

